name: Validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate scripts and model configuration
        shell: pwsh
        run: |
          ./tests/validate.ps1
